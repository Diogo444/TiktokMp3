PORT=3000
NODE_ENV=development

# Requis pour la conversion YouTube -> MP3 (si FFmpeg n'est pas dans le PATH)
# FFMPEG_PATH=C:\\tools\\ffmpeg\\bin\\ffmpeg.exe

# Bitrate audio MP3 pour YouTube (ex: 128k, 192k, 256k)
# YOUTUBE_AUDIO_BITRATE=192k

# Backend YouTube:
# - "yt-dlp" (recommandé si l'IP n'est pas bloquée)
# - "ytdl-core" (fallback Node, utile quand yt-dlp est bloqué)
# - "auto" (essaie yt-dlp si dispo, sinon ytdl-core)
# YOUTUBE_PROVIDER=auto

# Timeout (ms) pour yt-dlp
# YTDLP_TIMEOUT_MS=45000

# Optionnel : cookies YouTube (utile sur certains VPS / IP datacenter pour éviter le blocage "not a bot")
# Exportez un cookies.txt (format Netscape) depuis votre navigateur et montez-le dans le conteneur, puis :
# YTDLP_COOKIES_FILE=/run/secrets/youtube-cookies.txt
